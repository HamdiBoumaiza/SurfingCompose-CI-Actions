name: Checkout and set java version
description: Sets up Java
runs:
  using: "composite"
  steps:
    - name: use JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle
